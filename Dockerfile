# Deprecated monolithic Dockerfile.
# Build and run separately:
#   Detector (GPU): docker build -f Server/Dockerfile -t advertise-detector-server .
#   Browser UI:     docker build -f Client/Dockerfile -t advertise-detector-web .
# Or: docker compose build && docker compose up
